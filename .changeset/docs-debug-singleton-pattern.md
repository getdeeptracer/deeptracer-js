---
"@deeptracer/nextjs": patch
"@deeptracer/core": patch
---

Add test coverage for module-level `withContext` child flushed via `forRequest` sibling (Better Auth `sendOTP` pattern), and add README documentation for diagnosing silent log drops: `debug: true` usage guide and multi-logger-instance anti-pattern with fix.
